export class Car {

    private nm = 'android'

    constructor(
        private age = 10
    ) {
        this.nm = 'android'
    }
    prntAg() {
        console.log(`Your age is ${this.age}`)
    }
}

let byRm = 'android'

var cr = new Car()

