import { MyStrDirective } from './my-str.directive';

describe('MyStrDirective', () => {
  it('should create an instance', () => {
    const directive = new MyStrDirective();
    expect(directive).toBeTruthy();
  });
});
