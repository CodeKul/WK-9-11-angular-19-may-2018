import { MyAttrDirective } from './my-attr.directive';

describe('MyAttrDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAttrDirective();
    expect(directive).toBeTruthy();
  });
});
